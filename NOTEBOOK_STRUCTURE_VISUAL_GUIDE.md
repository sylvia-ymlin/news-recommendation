# 📍 Notebook结构 - 可视化指南

## 原始Notebook结构

```
新闻推荐系统-多路召回.ipynb (原始)
├── 📌 多路召回：概念介绍
├── 📦 Google Drive挂载 + 路径配置
├── 📚 导包 (pandas, numpy, faiss, etc.)
├── 📖 读取数据
│   ├── get_all_click_sample()
│   ├── get_all_click_df()
│   ├── get_item_info_df()
│   └── get_item_emb_dict()
├── 🔧 工具函数
│   ├── get_user_item_time()
│   ├── get_item_user_time()
│   ├── get_hist_and_last_click()
│   └── ...
├── 📊 定义多路召回字典
├── 🎯 召回效果评估函数
├── 🔢 计算相似矩阵
│   ├── ItemCF i2i_sim
│   ├── UserCF u2u_sim
│   └── Embedding i2i_sim
├── 🚀 召回实现
│   ├── YouTubeDNN召回
│   ├── ItemCF召回
│   ├── Embedding召回
│   └── UserCF召回
├── ❄️ 冷启动处理
└── 🔀 多路召回合并
    └── combine_recall_results()
```

---

## ✨ 新增部分（在最底部）

```
新闻推荐系统-多路召回.ipynb (升级后)
...
└── 🔀 多路召回合并
    └── combine_recall_results()

    ⬇️ ⬇️ ⬇️ 新增部分从这里开始 ⬇️ ⬇️ ⬇️

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 65: Markdown                            │
    │ # 🎯 多路召回策略升级                             │
    │ 说明新方法和预期效果                              │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 66: Python                              │
    │ # Step 1: 导入多路召回模块                        │
    │ from multi_strategy_recall import ...           │
    │ ✅ 耗时: <1分钟                                   │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 67: Python                              │
    │ # Step 2: 训练 ItemCF 协同过滤召回               │
    │ itemcf_recall = ItemCFRecall(...)              │
    │ itemcf_recall.fit(all_click_df)                │
    │ ⏱️ 耗时: 15-20分钟                               │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 68: Python                              │
    │ # Step 3: 训练 Embedding 向量检索召回            │
    │ embedding_recall = EmbeddingRecall(...)        │
    │ embedding_recall.fit(all_click_df, emb_df)     │
    │ ⏱️ 耗时: 10-15分钟                               │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 69: Python                              │
    │ # Step 4: 训练 Popularity 热门推荐召回           │
    │ popularity_recall = PopularityRecall(...)      │
    │ popularity_recall.fit(all_click_df)            │
    │ ⏱️ 耗时: 2-3分钟                                 │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 70: Python                              │
    │ # Step 5: 创建多路召回融合                        │
    │ fusion = RecallFusion(                         │
    │     strategies={...},                          │
    │     weights={...}                              │
    │ )                                              │
    │ ⏱️ 耗时: <1分钟                                   │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 71: Python                              │
    │ # Step 6: 为所有用户生成召回结果                  │
    │ final_recall_results = fusion.predict_batch()  │
    │ ⏱️ 耗时: 5-10分钟                                │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 72: Python                              │
    │ # Step 7: 生成提交文件                           │
    │ submission_df.to_csv(                          │
    │     'submission_multi_strategy.csv'            │
    │ )                                              │
    │ ⏱️ 耗时: 2-3分钟                                 │
    └─────────────────────────────────────────────────┘

    ┌─────────────────────────────────────────────────┐
    │ 📍 Cell 73: Markdown                            │
    │ ## ✅ 多路召回策略集成完成！                      │
    │ 总结、参数说明、下一步                            │
    └─────────────────────────────────────────────────┘
```

---

## 🎯 如何快速定位新增部分

### 方法1: 搜索标题
在notebook中搜索（Ctrl+F / Cmd+F）：
```
🎯 多路召回策略升级
```

### 方法2: 滚动到底部
1. 打开notebook
2. 按 `Ctrl+End` (Windows) 或 `Cmd+↓` (Mac)
3. 看到emoji图标 🎯 就是新增部分

### 方法3: 通过Cell数量
- 原始notebook: 约64个cell
- 新增: 8个cell (1个markdown + 6个python + 1个markdown)
- 总计: 约72个cell

---

## 📊 执行流程图

```
┌─────────────────────────┐
│  打开 Notebook          │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  运行前面的数据加载部分   │ ⏱️ 15分钟
│  (Cell 1 - Cell 64)     │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  确认变量已加载:         │
│  ✓ all_click_df        │
│  ✓ item_emb_dict       │
│  ✓ save_path           │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  滚动到最底部            │
│  找到 🎯 标记            │
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 66: 导入模块      │ ⏱️ <1分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 67: ItemCF训练    │ ⏱️ 15-20分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 68: Embedding训练 │ ⏱️ 10-15分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 69: Popularity训练│ ⏱️ 2-3分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 70: 创建融合器     │ ⏱️ <1分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 71: 批量召回      │ ⏱️ 5-10分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  Cell 72: 生成提交文件   │ ⏱️ 2-3分钟
└───────────┬─────────────┘
            │
            ▼
┌─────────────────────────┐
│  ✅ 完成！               │
│  检查输出文件            │
└─────────────────────────┘
```

---

## 🔍 每个Cell的识别特征

### Cell 66 (导入)
```python
# 特征: import语句
from multi_strategy_recall import ItemCFRecall, ...
```

### Cell 67 (ItemCF)
```python
# 特征: ItemCFRecall类实例化
itemcf_recall = ItemCFRecall(
    sim_item_topk=100,
    ...
)
```

### Cell 68 (Embedding)
```python
# 特征: EmbeddingRecall类 + articles_emb_df
embedding_recall = EmbeddingRecall(...)
articles_emb_df = pd.DataFrame(...)
```

### Cell 69 (Popularity)
```python
# 特征: PopularityRecall类
popularity_recall = PopularityRecall(...)
```

### Cell 70 (Fusion)
```python
# 特征: RecallFusion类 + weights字典
fusion = RecallFusion(
    strategies={...},
    weights={'itemcf': 0.5, ...}
)
```

### Cell 71 (批量召回)
```python
# 特征: predict_batch调用
final_recall_results = fusion.predict_batch(all_users, ...)
```

### Cell 72 (生成文件)
```python
# 特征: submission_df + to_csv
submission_df.to_csv(
    save_path + 'submission_multi_strategy.csv',
    ...
)
```

---

## 📝 Cell依赖关系

```
Cell 66 (导入)
   │
   ├──▶ Cell 67 (ItemCF)
   │       │
   │       └──▶ Cell 70 (Fusion)
   │               │
   ├──▶ Cell 68 (Embedding)
   │       │
   │       └──▶ Cell 70 (Fusion)
   │               │
   └──▶ Cell 69 (Popularity)    └──▶ Cell 71 (批量召回)
           │                            │
           └──▶ Cell 70 (Fusion)        └──▶ Cell 72 (生成文件)
```

**重要**: 必须按顺序执行！不能跳过任何cell！

---

## ⚠️ 常见错误定位

### 错误1: 找不到新增部分
**症状**: 滚动到底部没看到 🎯 标记
**原因**: Notebook文件未保存或未刷新
**解决**: 关闭重新打开notebook

### 错误2: Cell编号不连续
**症状**: 看到 Cell [*] 或者编号跳跃
**原因**: 之前运行失败或部分运行
**解决**: 重启kernel，重新运行所有cell

### 错误3: 变量未定义
**症状**: NameError: name 'all_click_df' is not defined
**原因**: 未运行前面的数据加载部分
**解决**: 从头开始运行到Cell 64，确保数据加载完成

---

## 📋 检查清单

运行前检查：
- [ ] 已找到新增的8个cell
- [ ] 已确认emoji标记 🎯 可见
- [ ] 已运行前面的数据加载部分
- [ ] 已确认 all_click_df, item_emb_dict 已定义

运行中检查：
- [ ] Cell 66: ✅ 模块导入成功
- [ ] Cell 67: ✅ ItemCF训练完成，有输出统计
- [ ] Cell 68: ✅ Embedding训练完成
- [ ] Cell 69: ✅ Popularity训练完成
- [ ] Cell 70: ✅ 融合器创建成功
- [ ] Cell 71: ✅ 批量召回完成，有统计
- [ ] Cell 72: ✅ 文件生成成功，显示路径

运行后检查：
- [ ] 所有cell都显示 [数字]（不是 [*]）
- [ ] 没有红色错误信息
- [ ] 生成了 submission_multi_strategy.csv
- [ ] 文件大小约 15KB

---

## 🎉 成功标志

当你在Cell 72看到这样的输出，说明成功了：

```
============================================================
📝 生成提交文件...
============================================================

✅ 提交文件已生成
   - 文件路径: /content/drive/.../submission_multi_strategy.csv
   - 用户数: 200000
   - 文件大小: 15.23 KB

📊 前5行预览:
   user_id  article_1  article_2  article_3  article_4  article_5
0   123456     789012     456789     234567     890123     567890
1   234567     901234     567890     345678     012345     678901
2   345678     012345     678901     456789     123456     789012
3   456789     123456     789012     567890     234567     890123
4   567890     234567     890123     678901     345678     012345
```

---

**准备好了吗？现在就开始执行吧！🚀**
